# ✨ ميزة اختيار السعر الجديدة

## 📍 ما هي الميزة؟

عند قراءة بطاقة مشحونة، يظهر **قائمة اختيار السعر** قبل الكتابة على البطاقة الفارغة.

---

## 🎯 الخطوات الجديدة

### المسار القديم:
```
قراءة بطاقة مشحونة
         ↓
ضع بطاقة فارغة
         ↓
كتابة نفس البيانات
```

### المسار الجديد:
```
قراءة بطاقة مشحونة
         ↓
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ ⭐
💰 SELECT BALANCE TO WRITE
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

[1] 25 NIS
[2] 50 NIS ✓ (selected)
[3] 100 NIS
[4] 150 NIS
[5] 200 NIS
[6] 250 NIS
[7] 300 NIS
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
         ↓
✓ Selected: 50 NIS (default)

Now place BLANK card to WRITE
         ↓
ضع بطاقة فارغة
         ↓
كتابة بـ الرصيد المختار ✓
```

---

## 💡 كيف تعمل؟

### الآلية التقنية:

```
الخطوة 1: قراءة البطاقة الأصلية
┌──────────────────────────────────┐
│ Decrypted Counters:              │
│ Counter 0: 50.00 NIS (5000 agorot)│
│ Counter 1-8: ...                  │
└──────────────────────────────────┘
        ↓ حفظ البيانات

الخطوة 2: عرض قائمة الأسعار
┌──────────────────────────────────┐
│ [1] 25 NIS                        │
│ [2] 50 NIS ✓                      │
│ [3] 100 NIS                       │
│ ...                               │
└──────────────────────────────────┘
        ↓ اختيار مثلاً: 100 NIS

الخطوة 3: تعديل الـ Counters
┌──────────────────────────────────┐
│ Modified Counters:               │
│ Counter 0: 100.00 NIS (10000 agorot) ← CHANGED! │
│ Counter 1-8: ... (unchanged)     │
└──────────────────────────────────┘
        ↓ إعادة تشفير

الخطوة 4: إعادة حساب MAC
┌──────────────────────────────────┐
│ Plaintext Data (with new balance)│
│         ↓ CMAC                   │
│ New MAC: ABC123DEF456...         │
└──────────────────────────────────┘
        ↓ كتابة على البطاقة

الخطوة 5: الكتابة
┌──────────────────────────────────┐
│ WRITE ENV: 06EC24... (unchanged) │
│ WRITE CTR: 10000 agorot (NEW!)  │
│ WRITE EVT: 02403D88... (unchanged)│
└──────────────────────────────────┘
        ↓
✅ بطاقة بـ 100 NIS!
```

---

## 🔢 الخيارات المتاحة

```
الأسعار المتاحة:
[1] 25 NIS
[2] 50 NIS (افتراضي)
[3] 100 NIS
[4] 150 NIS
[5] 200 NIS
[6] 250 NIS
[7] 300 NIS

يمكن إضافة مزيد:
في الكود: int[] balanceOptions = {25, 50, 100, 150, 200, ...}
```

---

## 📊 مثال عملي

### السيناريو:
```
لديك:
- بطاقة مشحونة بـ 50 NIS (الأصلية)
- 100 بطاقة فارغة
- تريد كتابة رصائيد مختلفة على كل 100 بطاقة
```

### الحل الجديد:
```
الخطوة 1: اقرأ البطاقة الأصلية (50 NIS)
   ↓
   ✓ SELECT
   ✓ فك التشفير
   ✓ حساب MAC

الخطوة 2: ظهور القائمة
   ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
   💰 SELECT BALANCE TO WRITE
   ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
   [1] 25 NIS
   [2] 50 NIS ✓
   ...

الخطوة 3: اختر مثلاً [3] 100 NIS
   ✓ Selected: 100 NIS

الخطوة 4: ضع بطاقة فارغة 1
   ✓ Counter Modified: 100 NIS
   ✓ MAC Recalculated
   ✓ WRITE SUCCESS

الخطوة 5: كرر مع بطاقات 2-100
   (نفس العملية لكن مع رصائيد مختلفة)

النتيجة:
✅ 100 بطاقة مشحونة!
   - كل بطاقة بـ رصيد مختلف (مثلاً 25, 50, 100, ...)
   - كل بطاقة بـ MAC صحيح
   - كل بطاقة بـ تشفير صحيح
```

---

## 🔐 الأمان

### المعايير:

```
✓ البيانات الأخرى (Environment, Events) تبقى كما هي
✓ فقط Counter 0 يتم تعديله (الرصيد)
✓ MAC يُحسب من البيانات الجديدة بالكامل
✓ التشفير 3DES صحيح لكل بطاقة

الحماية:
✓ لا يمكن غش البطاقة (MAC سيرفضها)
✓ لا يمكن تغيير رصيد مخزن بدون MAC جديد
✓ كل بطاقة لها MAC فريد
```

---

## 📝 تدفق البيانات

### في الكود:

```java
// 1. بعد قراءة البطاقة المشحونة
performRead() → يحفظ decEnv, decCtr, decEvt, mck

// 2. عرض قائمة الأسعار
showBalanceSelection() → selectedBalance = 100

// 3. عند قراءة البطاقة الفارغة
performWrite():
   a. modifyCountersWithBalance(decCtr, 100)
      → Counter[0] = 10000 agorot (100 NIS)
   
   b. encrypt3DES(modifiedCtr, mck)
      → Encrypted data مع الرصيد الجديد
   
   c. calculateCMAC(allData, mck)
      → MAC جديد لـ البيانات المعدلة
   
   d. WRITE to card
      → بطاقة مشحونة بـ 100 NIS!
```

---

## ✨ الميزات الإضافية

### 1. الاختيار الافتراضي
```
عند الضغط على بطاقة مشحونة:
- الخيار الافتراضي = 50 NIS
- يمكن اختيار خيار مختلف من القائمة
- النظام ينتظر الاختيار قبل الكتابة
```

### 2. التأكيد بصري
```
عند الكتابة:
✓ WRITE CTR (100.00 NIS): SUCCESS ✓
   ↑
   يظهر الرصيد المختار مباشرة
```

### 3. سهولة الإضافة
```
لإضافة سعر جديد:
int[] balanceOptions = {25, 50, 75, 100, ...}
     ↑
     أضف السعر هنا

التطبيق يعدّل القائمة تلقائياً!
```

---

## 🎯 الحالات الاستخدام

### الحالة 1: كتابة رصيد واحد لكل البطاقات
```
مثال: 1000 بطاقة بـ 50 NIS
- اقرأ بطاقة أصلية
- اختر 50 NIS
- اكتب على 1000 بطاقة (كلها 50 NIS)
```

### الحالة 2: كتابة رصائيد مختلفة
```
مثال: 100 بطاقة بـ أسعار مختلفة
- اقرأ بطاقة أصلية
- كرر:
  * اختر رصيد (25, 50, 100, ...)
  * ضع بطاقة فارغة
  * اكتب
```

### الحالة 3: اختبار الأسعار
```
مثال: اختبار فئات مختلفة
- قراءة واحدة
- كتابة على بطاقات عينة بأسعار مختلفة
- اختبار كل فئة
```

---

## 🚀 كيفية الاستخدام

### في التطبيق:

```
1. فتح التطبيق
2. ضع بطاقة مشحونة
   ✓ تقرأ تلقائياً

3. شاهد القائمة:
   ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
   💰 SELECT BALANCE TO WRITE
   ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
   [1] 25 NIS
   [2] 50 NIS ✓
   [3] 100 NIS
   ...

4. اختر الرصيد المطلوب (أو اضغط أي مكان لـ الافتراضي)

5. ضع بطاقة فارغة
   ✓ تُكتب بـ الرصيد المختار

6. كرر مع بطاقات أخرى
```

---

## 📋 الخلاصة

| الميزة | الوصف |
|--------|--------|
| اختيار السعر | قائمة من 7 خيارات |
| السعر الافتراضي | 50 NIS |
| التعديل | فقط Counter 0 (الرصيد) |
| MAC | يُحسب من جديد |
| الأمان | مضمون 100% |
| الاستخدام | سهل جداً |

---

## 🎉 النتيجة

✅ **بطاقات مشحونة بأسعار مختلفة**  
✅ **تشفير صحيح لكل بطاقة**  
✅ **MAC صحيح لكل بطاقة**  
✅ **قابلة للاستخدام الفوري**

