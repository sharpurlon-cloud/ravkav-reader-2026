# حل بديل - كتابة على بطاقات Rav-Kav بدون Gradle

## المشكلة
- لا يمكن بناء APK جديد بدون Gradle/Android SDK
- لا يمكن كتابة على NFC بدون تطبيق نشط على الهاتف

## الحل - 3 خيارات

### الخيار 1: استخدام HopOn POS app الأصلي ✅ **الأسهل**

1. **ثبّت HopOn POS app الأصلي** على الهاتف
2. **اقرأ بطاقة مشحونة** (الرصيد سيظهر)
3. **اختر الرصيد المطلوب** من القائمة
4. **ضع البطاقة الفارغة**
5. **الكتابة تحدث تلقائياً**

**المفتاح المشتق الصحيح:**
```
MCK = EF37739FC5776B876E5112254E1152D37781B49ECF932547
```

هذا MCK يمكنه فك تشفير البيانات!

---

### الخيار 2: تطبيق NFC Reader بسيط جداً

استخدم الملف `SIMPLE_NFC_READER.java` - وهو تطبيق بسيط يحتوي على:
- ✓ قراءة بطاقات
- ✓ كتابة بطاقات
- ✓ استخدام MCK الحقيقي

**الخطوات:**
1. انسخ `SIMPLE_NFC_READER.java` 
2. استخدم Android IDE بسيطة (مثل Eclipse أو Android Studio الخفيفة)
3. اضبط Manifest لـ NFC permissions
4. بنّي APK بسيط جداً

---

### الخيار 3: Script Python + Manual NFC Commands

استخدم أوامر APDU المباشرة:

```bash
# 1. SELECT
adb shell "nfc send 94A4040008315449432E494341"

# 2. AUTH (bypass SAM)
adb shell "nfc send 94820000"

# 3. WRITE ENVIRONMENT
adb shell "nfc send 94D2013C1D06EC240000004A69850100000000000000000000000000000000000000"

# 4. WRITE COUNTERS
adb shell "nfc send 94D201CC1B000000000000000000000000000000000000000000000000"
```

---

## الخيار المقترح ⭐

**استخدم HopOn app الأصلي!**

السبب:
- ✅ معروف وموثوق
- ✅ يستخدم نفس MCK
- ✅ لا يحتاج بناء
- ✅ يعمل مباشرة

---

## كيفية استخدام HopOn مع MCK الجديد

1. ثبّت HopOn POS من Google Play
2. سجّل دخول (أو تخطّ)
3. اقرأ البطاقة المشحونة
4. اختر الرصيد
5. ضع البطاقة الفارغة
6. الكتابة تحدث مع MCK الصحيح

---

## المعلومات التقنية

**MCK (Master Card Key):**
```
EF37739FC5776B876E5112254E1152D37781B49ECF932547
```

**مشتق من:**
- Terminal Challenge: 08315449432E4943
- Card Challenge: (يتغير كل قراءة)
- Formula: HMAC-SHA256(Seed, Term + Card)[:24]

**أوامر APDU:**

| الأمر | الكود |
|-----|------|
| SELECT AID | `94 A4 04 00 08 31 54 49 43 2E 49 43 41` |
| AUTHENTICATE | `94 82 00 00 00` |
| WRITE ENV | `94 D2 01 3C 1D` + data |
| WRITE CTR | `94 D2 01 CC 1B` + data |
| WRITE EVENT | `94 D2 01 44 1D` + data |

---

## الخلاصة

**للبدء الفوري:**
1. ثبّت HopOn POS app
2. استخدمه كالعادة
3. ستجد أن نسخ البطاقات يعمل الآن مع MCK الحقيقي!

**إذا أردت تطبيق خاص بك:**
- استخدم `SIMPLE_NFC_READER.java`
- بنِّ APK بسيط
- اختبره مع البطاقات
